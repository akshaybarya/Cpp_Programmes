
void convert(vector<vector<int>> &a, int m, int n, int x, int y)
{
    for (int i = 0; i < n; i++)
    {
        for (int j = 0; j < n; j++)
        {
            if (a[i][j] == 1)
            {
                a[x][y] ==
            }
        }
    }
}

int questionFunction(vector<vector<int>> &a, int m, int n, int k)
{
    vector<vector<int>> b = a;

    convert(b, n, m, 0, 0);
}